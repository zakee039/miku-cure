# Runtime dependencies. Keep exact pins in sync with portable-build validation.
# MediaPipe depends on the contrib build; never install a second cv2 distribution.
opencv-contrib-python==4.13.0.92
mediapipe==0.10.35
numpy==2.5.0
websockets==16.0
openai==2.43.0
python-dotenv==1.2.2
Pillow==12.3.0

# PyTorch is installed separately from the official CPU wheel index by install.bat.
# torch==2.12.1+cpu
# torchvision==0.27.1+cpu

# Training and analysis dependencies are intentionally not part of the runtime.
# Set MIKU_INSTALL_TRAINING=1 before running install.bat to install them.
